<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.leyizhuang.app.foundation.dao.StorePreDepositLogDAO">


    <select id="findByUserIdAndType" resultType="cn.com.leyizhuang.app.foundation.pojo.response.PreDepositLogResponse">
        SELECT
        l.id, l.type, l.create_time, l.change_money, l.order_number
        FROM st_pre_deposit_log l
        LEFT JOIN emp_employee e ON e.store_id = l.store_id AND e.emp_id = #{userId}
        WHERE
        l.type in
        <foreach collection="list" item="type" separator="," open="(" close=")">
            #{type}
        </foreach>
    </select>

    <insert id="save" parameterType="cn.com.leyizhuang.app.foundation.pojo.StPreDepositLogDO">
        INSERT INTO cus_pre_deposit_log
        (create_time, change_money, remarks, type, store_id, operator_id, operator_type,
        operator_ip, order_number, balance, detail_reason, transfer_time, user_order_number)
        VALUES
        (createTime, changeMoney, remarks, type, storeId, operatorId, operatorType,
        operatorIp, orderNumber, balance, detailReason, transferTime, userOrderNumber)
    </insert>

</mapper>