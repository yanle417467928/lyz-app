<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.leyizhuang.app.foundation.dao.UserDAO">

    <resultMap id="userVoResultMap" type="UserVo">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="login_name" property="loginName" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="TINYINT"/>
        <result column="age" property="age" jdbcType="TINYINT"/>
        <result column="user_type" property="userType" jdbcType="TINYINT"/>
        <result column="status" property="status" jdbcType="BIT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>

        <collection property="rolesList" ofType="Role">
            <id column="ROLE_ID" property="id"/>
            <result column="ROLE_NAME" property="name"/>
        </collection>
    </resultMap>

   <select id="selectByLoginName" parameterType="UserVO" resultType="User">
        SELECT * FROM user where LOGIN_NAME = #{loginName};
   </select>
    <select id="queryList" resultType="Resource">
        SELECT *
        FROM user
        ORDER BY CREATE_TIME DESC
    </select>
    <select id="queryListVO" resultMap="userVoResultMap">
        SELECT
        t.ID ID,
        t.LOGIN_NAME LOGIN_NAME,
        t.NAME NAME,
        t.PASSWORD PASSWORD ,
        t.SEX SEX,
        t.AGE AGE,
        t.USER_TYPE USET_TYPE,
        t.STATUS STATUS,
        t.CREATE_TIME CREATE_TIME,
        t.PHONE PHONE,
        o.ID AS ROLE_ID,
        o.NAME AS ROLE_NAME
        FROM
        user t
        LEFT JOIN user_role r ON t.id = r.user_id
        LEFT JOIN role o ON r.role_id = o.id
        <where>
            <if test=" name != null and name != '' ">
                t.name = #{name}
            </if>
            <if test=" startTime != null ">
                <![CDATA[ and t.create_time >= #{startTime} ]]>
            </if>
            <if test=" endTime != null ">
                <![CDATA[ and t.create_time <= #{endTime} ]]>
            </if>
        </where>
    </select>
    <select id="queryById" parameterType="java.lang.Long" resultType="User">
        select * from user where ID = #{id};
    </select>

    <insert id="save" parameterType="User">
        INSERT INTO user( LOGIN_NAME,NAME,PASSWORD,SEX,AGE,PHONE,USER_TYPE,STATUS,CREATE_TIME)
        VALUES (#{loginName}, #{name}, #{password}, #{sex}, #{age}, #{phone}, #{userType}, #{status}, #{createTime})
    </insert>

</mapper>